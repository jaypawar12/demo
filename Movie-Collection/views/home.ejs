<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MovieMania</title>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <style>
        :root {
            --dark-bg: #141414;
            --light-bg: #ffffff;
            --primary: #e50914;
            --secondary: #222222;
            --text-light: #fff;
            --card-bg: rgba(0, 0, 0, 0.8);
            --btn-hover: #b20710;
            --warning: #f9d923;
            --success: #08f800;
        }

        body {
            background-color: var(--dark-bg);
            color: var(--text-light);
            font-family: 'Roboto', sans-serif;
        }

        /* Navbar */
        .navbar {
            backdrop-filter: blur(10px);
            background-color: rgba(0, 0, 0, 0.85);
            transition: all 0.3s ease;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 2rem;
            color: var(--primary) !important;
        }

        .nav-link {
            color: #fff !important;
            font-weight: 500;
            margin-left: 1rem;
        }

        .nav-link:hover {
            color: var(--primary);
        }

        /* Banner Section */
        .banner {
            background: linear-gradient(to right, rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
                url('https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTPBlq5Tm0pH6vBuP6A90-C_WcUBDnR3i7RrQ&s') center/cover no-repeat;
            padding: 300px 0;
            text-align: center;
            color: white;
        }

        .banner h1 {
            font-size: 4rem;
            font-weight: 800;
            text-transform: uppercase;
        }

        .banner p {
            font-size: 1.4rem;
        }

        .btn-glow {
            background-color: var(--primary);
            border: none;
            color: white;
            padding: 12px 28px;
            font-weight: 600;
            border-radius: 30px;
            transition: all 0.3s ease-in-out;
        }

        .btn-glow:hover {
            box-shadow: 0 0 15px var(--primary);
            transform: scale(1.05);
        }

        /* Movie Cards */
        .movie-row {
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
            justify-content: center;
        }

        .movie-card {
            background: var(--card-bg);
            border-radius: 12px;
            overflow: hidden;
            backdrop-filter: blur(8px);
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .movie-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.25);
        }

        .img-body {
            width: 100%;
            height: 250px;
        }

        .card-img-top {
            height: 100%;
            object-fit: cover;
            object-position: center;
            border-radius: 12px 12px 0 0;
        }

        .card-body {
            display: flex;
            flex-direction: column;
            padding: 20px;
            color: var(--text-light);
        }

        .card-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .card-body p {
            margin-bottom: 10px;
            color: #ddd;
        }

        .rating i {
            font-size: 1.1rem;
            color: var(--warning);
        }

        .card-body .d-flex {
            margin-top: auto;
        }

        .card-body .btn {
            width: 45%;
            padding: 8px;
            font-size: 0.9rem;
            border-radius: 8px;
        }

        .card-body .btn-warning {
            background-color: var(--warning);
            border: none;
        }

        .card-body .btn-danger {
            background-color: var(--primary);
            border: none;
        }

        .card-body .btn:hover {
            transform: scale(1.05);
        }

        .btn-edit {
            background-color: var(--success);
            border: none;
        }

        .btn-edit:hover {
            color: black;
            background-color: var(--success);
            transform: scale(1.05);
        }

        /* Features Section */
        .features-section {
            background-color: var(--secondary);
            padding: 80px 0;
        }

        .features-section h2 {
            font-size: 2.5rem;
            margin-bottom: 40px;
            color: var(--primary);
        }

        .feature-card {
            background: var(--light-bg);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            transition: 0.3s ease-in-out;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.2);
        }

        .feature-card i {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 20px;
        }

        .feature-card h5 {
            font-size: 1.6rem;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .feature-card p {
            font-size: 1rem;
        }

        footer {
            background-color: var(--dark-bg);
            color: #aaa;
            padding: 20px 0;
            font-size: 1rem;
            text-align: center;
        }

        .social-icons a {
            color: #ccc;
            font-size: 1.6rem;
            margin: 0 15px;
            transition: 0.3s;
        }

        .social-icons a:hover {
            color: var(--primary);
        }
    </style>
</head>

<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="#">🎬 MovieMania</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto text-uppercase">
                    <li class="nav-item"><a class="nav-link" href="#">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/form">Add Movie</a></li>
                    <li class="nav-item"><a class="nav-link" href="#features">Features</a></li>
                    <li class="nav-item"><a class="nav-link" href="#movieInventory">Collection</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Banner -->
    <section class="banner">
        <div class="container">
            <h1>Welcome to MovieMania</h1>
            <p class="mb-4">Your stylish personal movie library</p>
            <a href="/form" class="btn btn-glow">Add New Movie</a>
        </div>
    </section>

    <!-- Movie Collection -->
    <h2 class="text-center my-5 fw-bold text-warning" id="movieInventory">Movie Collection</h2>
    <div class="movie-row">
        <% record.forEach((e)=> { %>
            <div class="col-md-2 mb-4">
                <div class="card movie-card h-100">
                    <div class="img-body">
                        <a href="/movieShow/<%= e.id %>">
                            <img src="<%= e.movieImage %>" class="card-img-top" />
                        </a>
                    </div>
                </div>
            </div>
            <% }) %>
    </div>

    <!-- Features Section -->
    <section class="features-section" id="features">
        <div class="container text-center text-dark">
            <h2 class="fw-bold text-warning">Why Use MovieMania?</h2>
            <div class="row">
                <div class="col-md-4 mb-4">
                    <div class="feature-card">
                        <i class="bi bi-collection-play-fill"></i>
                        <h5>Your Personal Movie Shelf</h5>
                        <p>Track all your movies in one place — from new releases to your classics. All in a
                            beautifully.</p>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="feature-card">
                        <i class="bi bi-pencil-square"></i>
                        <h5>Full Control</h5>
                        <p>Easily add, update, and delete movies. Keep your collection exactly how you want it.</p>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="feature-card">
                        <i class="bi bi-star-fill"></i>
                        <h5>Rate & Sort</h5>
                        <p>Rate your movies and sort them by your preferences — genre, rating, or price.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <p>© 2025 MovieMania — All Rights Reserved</p>
            <div class="social-icons mt-2">
                <a href="#"><i class="bi bi-instagram"></i></a>
                <a href="#"><i class="bi bi-github"></i></a>
                <a href="#"><i class="bi bi-linkedin"></i></a>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>